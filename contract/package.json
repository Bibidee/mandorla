{
  "name": "mandorla-contract",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "deploy": "ts-node --project tsconfig.json deploy.ts",
    "deploy:local": "GENLAYER_NETWORK=localnet ts-node --project tsconfig.json deploy.ts"
  },
  "dependencies": {
    "genlayer-js": "1.1.8"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.5.0"
  }
}
